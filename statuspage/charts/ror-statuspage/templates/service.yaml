apiVersion: v1
kind: Service
metadata:
  name: {{ include "ror-statuspage.fullname" . }}
  labels:
    {{- include "ror-statuspage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ror-statuspage.selectorLabels" . | nindent 4 }}
