name: Final release promotion not enabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'Legacy tag publication is disabled. Use Release Candidate for tested RCs. Final approval-gated promotion is not enabled yet.' >&2
          exit 1